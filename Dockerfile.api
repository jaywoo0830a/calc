FROM node:24.18.0-alpine

# ── PDF → Markdown 변환 스택 ──
# poppler-utils: pdftotext (PDF → HTML 추출)
# pandoc + pandoc-cli: 문서 변환기 (Haskell 풀버전)
RUN apk add --no-cache \
    poppler-utils \
    pandoc \
    pandoc-cli \
    python3 \
    make \
    g++

WORKDIR /app
COPY server/package.json ./
RUN npm install --legacy-peer-deps
COPY server/ .

EXPOSE 3001
CMD ["node", "index.js"]
