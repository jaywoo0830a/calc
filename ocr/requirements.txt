# CPU용 torch 핀 — PyPI에서 CUDA 휠이 섞이지 않게 extra-index로 cpu만 사용
--extra-index-url https://download.pytorch.org/whl/cpu
torch==2.13.0+cpu
torchvision==0.28.0+cpu

# PaddleOCR-VL은 최신 transformers 필요 (모델 카드 권장 git 설치)
transformers @ git+https://github.com/huggingface/transformers.git
pillow
fastapi
uvicorn[standard]
