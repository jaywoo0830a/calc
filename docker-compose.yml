services:
  calc:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: calc
    ports:
      - "8080:80"
    volumes:
      # npm 캐시 볼륨 — decimal.js 재다운로드 방지
      - npm_cache:/root/.npm
    restart: unless-stopped

volumes:
  npm_cache:
    driver: local
