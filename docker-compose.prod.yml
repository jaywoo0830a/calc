services:
  caddy:
    image: caddy:alpine
    container_name: calc-caddy
    ports:
      - "80:80"
      - "443:443"
      - "443:443/udp"   # HTTP/3(QUIC) — UDP 443 노출로 최신 브라우저 H3 활성
    extra_hosts:
      - "host.docker.internal:host-gateway"   # freedf nginx(호스트 8081)로 프록시용
    volumes:
      - ./Caddyfile:/etc/caddy/Caddyfile:ro
      - caddy_data:/data
      - caddy_config:/config
    restart: unless-stopped
    depends_on:
      - calc

volumes:
  caddy_data:
    driver: local
  caddy_config:
    driver: local
